spring:
  application:
    name: message-validator
  cloud:
    config:
      uri: http://localhost:7070
server:
  port: 5050
management:
  endpoints:
    web:
      exposure:
        include: refresh,info,health,metrics