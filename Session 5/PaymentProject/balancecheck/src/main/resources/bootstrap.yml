spring:
  application:
    name: balance-check
  cloud:
    config:
      uri: http://localhost:7070
server:
  port: 6060
management:
  endpoints:
    web:
      exposure:
        include: refresh,info,health,metrics