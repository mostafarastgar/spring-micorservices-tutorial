info:
  app:
    name: "Balance Checker service"
    description: "This service can check banks available balance"
    version: "1.0.0"
spring:
  application:
    name: balance-check
  cloud:
    config:
      uri: http://localhost:7070
server:
  port: 6060
